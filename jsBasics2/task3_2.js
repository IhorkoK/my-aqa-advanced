const tableValue = 5;
let tableValueRes;
let operand = 1;
while (operand < 11){
    tableValueRes = tableValue * operand;
    console.log( tableValue + " X " + operand + " = " + tableValueRes)
    operand++;
}
